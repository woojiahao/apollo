import React, { PropsWithChildren } from "react"

const SidebarActions = ({ children }: PropsWithChildren<any>) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default SidebarActions